import 'package:flutter/material.dart';

class VerifyphoneOrAadhaarNoScreen extends StatelessWidget {
  const VerifyphoneOrAadhaarNoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This Screen is not ready yes'),
    );
  }
}
